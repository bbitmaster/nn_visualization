This is a python program designed to do a visualization of a neural network.

It uses my own neural network toolkit, and opencv to draw to a window
(probably not the best choice, I may change that later but matplotlib was just too slow)
